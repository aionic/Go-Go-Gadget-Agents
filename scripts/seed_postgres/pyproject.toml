[project]
name = "seed-postgres"
version = "0.1.0"
description = "Post-provision schema build + sample data seeding for the agents demo database."
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2",
    "faker>=30.0",
]

[tool.uv]
package = false
